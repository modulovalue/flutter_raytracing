name: flutter_raytracing
description: A new Flutter project.
version: 1.0.0+1

environment:
  sdk: ">=2.1.0 <3.0.0"

dependencies:
  flutter:
    sdk: flutter
  bird: ^0.0.3+1
  bird_flutter: ^0.0.3
  image: ^2.1.4

dev_dependencies:
  extra_pedantic: ^1.1.1+3
  flutter_test:
    sdk: flutter

flutter:
  uses-material-design: true

  assets:
    - assets/scenes/
    - assets/scenes/backgrounds/
    - assets/scenes/textures/